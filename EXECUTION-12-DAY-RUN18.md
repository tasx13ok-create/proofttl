# ProofTTL 12-Day Execution — Run 18

## 2026-09-05 COMPLETE EVIDENCE-STAGE RECEIPT CHECKPOINT

- Re-inspected the receipt-derived Evidence Plan boundary after discovery-completion hardening and found another false-completion class: a real `PLANNED` Evidence Plan could report `COMPLETE` after a completed candidate query alone, even though every executable plan also budgets source fetches and semantic evaluations. That would allow future orchestration to claim EVIDENCE -> VERDICT completion without proving that a discovered candidate was ever fetched or semantically evaluated.
- Tightened `summarizeEvidenceExecution` so a positive planned `max_source_fetches` budget requires at least one settled `COMPLETED` `SOURCE_FETCH` receipt, and a positive planned `max_semantic_evaluations` budget requires at least one settled `COMPLETED` `SEMANTIC_EVALUATION` receipt. Missing stages now report `SOURCE_FETCH_INCOMPLETE` or `SEMANTIC_EVALUATION_INCOMPLETE` instead of `COMPLETE`.
- Extended the execution summary contract with explicit `source_fetch_required/completed` and `semantic_evaluation_required/completed` fields. The final verification-outcome boundary now recognizes both new incomplete states, so they continue through the existing fail-closed semantics: the evidence-level verdict remains auditable, while the published final verdict/confidence stay withheld until planned execution is complete.
- Added regressions proving candidate-only execution cannot skip fetch, candidate+fetch cannot skip semantic evaluation, and candidate+fetch+semantic can complete a non-adversarial budgeted plan. The first CI pass (`33966959186`) correctly caught one older plan-binding replay fixture that still assumed a lone source fetch represented complete execution; the fixture was repaired by adding the required semantic receipt while preserving the replay/deduplication assertion rather than weakening production completion rules.
- Full `ProofTTL Code Checks` are green at code checkpoint `e382823d86bcaa6e7394ae6692bd0931255fc08e` (Actions run `33967050700`). The corrected run passed the core security/limits/economics/lease and evidence suites, commercial/account/platform checks, assistant/entitlement checks, readiness/auth/routing/payment/research/regression checks, and Worker dry-run validation.
- Final diff review from prior sprint head `e4b84dd4806e15db5497a5b76e944adff67ae1a6` is limited to the evidence execution summary, recognition of its two new incomplete statuses, the direct execution-summary/plan-binding regressions, and this run log. The change is additive and reversible; no provider, threshold, payment, monitor, or UI behavior was broadened.
- Rechecked `proofttl-web`: sprint and `main` remain identical at `ce43f9eb4000048547e3451aac836c68da0678dd`. Live Vercel inspection confirms the matching sprint preview `dpl_G9gffm5Dka7uMmLyxskp2PwYYGX9` and production deployment `dpl_6HELAY62mzgDuNRmQ8KNBCFrAXpK` are both `READY`, so no unrelated web churn was introduced.
- Remaining flagship truth boundary: public `/verify` still must execute a real provenance-preserving independent candidate discovery provider, bounded source fetches, semantic evidence evaluation, and—when required—a genuinely separate adversarial contradiction retrieval pass through the budgeted executor. With this checkpoint, query-only orchestration cannot be mislabeled as a complete evidence run; definitive automated final verdicts still require valid receipts for the substantive evidence stages.
