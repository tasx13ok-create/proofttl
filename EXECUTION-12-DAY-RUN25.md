# ProofTTL 12-Day Execution Log — Run 25

## 2026-09-05 COST-CAPPED EVIDENCE PIPELINE CHECKPOINT

- Re-inspected the hardened evidence runtime after adversarial retrieval preservation and found a budget-ordering correctness gap. With a caller-tightened `hard_cost_ceiling_usd`, the orchestrator previously executed every selected `SOURCE_FETCH` before any `SEMANTIC_EVALUATION`. A cap that was sufficient for at least one complete fetch -> semantic evidence pipeline could therefore be exhausted on raw retrievals first, leaving zero evaluated evidence.
- Changed evidence orchestration to interleave semantic evaluation immediately after each successful source fetch. The executor's hard cost ceiling, action-count limits, idempotency receipts, exact source binding, public-source validation, adversarial candidate preservation, and fail-closed verdict semantics are unchanged. The change only makes spend ordering coherent with the product goal: prioritize completed usable evidence over unevaluated fetched documents when runtime cost is tighter than the plan's worst-case budget.
- Added a regression with a `$0.009` runtime ceiling and current synthetic action pricing. After discovery consumes `$0.003`, the remaining budget can fund one `$0.002` fetch plus one `$0.003` semantic evaluation but not all planned work. The regression requires one evidence item to survive, requires fetch -> semantic provider call ordering, and still requires the overall execution to report `BUDGET_TRUNCATED` when later reservations are denied.
- Full `ProofTTL Code Checks` are green at substantive code checkpoint `ff830c681bcd80a7fb1ea978d244944ff953965f` (Actions run `33985948490`). Every job step completed successfully, including core evidence/security/economics gates and Worker bundle validation.
- Final web/deployment drift check found no justified UI mutation: `proofttl-web/10xeffort-12-day-sprint` and `main` remain identical at `ce43f9eb4000048547e3451aac836c68da0678dd`. The matching sprint preview deployment `dpl_G9gffm5Dka7uMmLyxskp2PwYYGX9` and production deployment `dpl_6HELAY62mzgDuNRmQ8KNBCFrAXpK` are both `READY`.
- Remaining flagship boundary is unchanged and intentionally not overstated: public `/verify` still uses the caller-provided-source path. Production independent discovery, bounded source retrieval, exact-source semantic evaluation, real runtime provider pricing/configuration, and a genuinely separate adversarial contradiction provider still need to be wired through this executor before ProofTTL can truthfully expose automated receipt-backed end-to-end evidence execution there.