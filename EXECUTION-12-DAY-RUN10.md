# ProofTTL 12-Day Execution Log — Run 10

## 2026-09-04 EVIDENCE EXECUTOR LATENCY FAIL-CLOSED CHECKPOINT

- Re-inspected the current sprint execution path from Claim Contract -> triage -> evidence plan -> evidence ledger -> final verification outcome and found a reliability gap adjacent to the still-missing live discovery/contradiction stage: `buildEvidencePlan` publishes an explicit `latency_budget_ms`, but the generic evidence executor enforced action counts and cost reservations only. A slow or hung candidate-query/source-fetch/semantic/contradiction provider could therefore exceed the declared plan deadline while remaining inside every other budget.
- Hardened `createEvidenceExecutor` with a single executor-wide deadline derived from the plan latency budget. The executor now checks the deadline before reservation, again before provider execution, and again before accepting a provider result. Once the deadline is exhausted, the budget closes fail-closed under `evidence_latency_budget_exceeded`; new actions are denied and late provider values are withheld.
- Provider execution now races the remaining evidence deadline and receives an `AbortSignal` plus the absolute deadline. Providers that honor cancellation can stop work immediately. Providers that ignore cancellation can no longer make their late value escape into the evidence path: the reservation is settled conservatively at its reserved cost, marked `TIMEOUT`, and the whole evidence budget is closed so no later retrieval/semantic/contradiction work can continue on a timed-out plan.
- Added deterministic regression coverage for a deadline exhausted before an action begins and an in-flight contradiction provider that runs past the executor deadline. The regressions assert zero provider calls after a pre-start expiry, conservative settlement of a timed-out reservation, `TIMEOUT` outcome, aborted provider signal, withheld late value, budget closure, and denial of further work.
- Code commits in this checkpoint: `2afa4b01000f88378fc813f30e244948d8a8f410` (executor deadline enforcement) and `5d4faa116ac36957183b50fac46796b240a703ea` (regression coverage). ProofTTL Code Checks run `33931732400` was still in progress during finalization; setup completed and dependency installation was still running, so this log intentionally does not claim green CI yet.
- Rechecked `proofttl-web`: `10xeffort-12-day-sprint` and `main` remain identical at `ce43f9eb4000048547e3451aac836c68da0678dd`. No visual changes were introduced because there was no concrete web regression to fix in this pass.
- Remaining flagship truth boundary is unchanged: public `/verify` still does not execute provenance-preserving independent source discovery plus a real adversarial contradiction retrieval pass. The generic executor is now safer for that integration because its declared latency budget is actually enforceable rather than advisory.
