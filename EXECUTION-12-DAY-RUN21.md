# ProofTTL 12-Day Execution — Run 21

## 2026-09-05 BOUNDED EVIDENCE ORCHESTRATION CHECKPOINT

- Re-inspected the flagship evidence boundary after the deploy-gate coherence checkpoint. The repository already had a Claim Contract, Evidence Plan, finite runtime-budget materializer, bounded executor, receipt-derived execution summary, evidence ledger, and fail-closed final outcome, but no single production primitive actually connected those components into a staged evidence run.
- Added `src/evidence-orchestrator.js`, a provider-agnostic orchestration boundary that now executes planned primary candidate discovery, a genuinely separate adversarial contradiction query when required, bounded source fetching, bounded semantic evaluation, and receipt-derived final outcome construction through the existing hardened executor.
- Runtime pricing is materialized before execution. Every action uses the existing reserve-cost/idempotency/latency/cost ceilings. Candidate URLs are normalized and deduplicated before fetch; discovery provenance (`PRIMARY_DISCOVERY` versus `ADVERSARIAL_CONTRADICTION`) is carried through fetch and into semantic evidence provenance.
- Added provider-contract guards around the executor so malformed candidate lists, source-fetch values, or semantic evidence items are converted into executor-visible provider failures rather than being allowed to satisfy stage completion with unusable values. Missing/failed stages therefore continue to fail closed to `UNKNOWN` under the existing execution-summary/outcome rules.
- Added `scripts/evidence-orchestrator-test.js` covering a complete high-assurance run, separate contradiction discovery flowing through fetch/evaluation, malformed discovery output failing closed, and a zero-dollar runtime ceiling denying execution.
- Code checkpoint `d02709f21d537dfa46fc675032a22de8208ddff0` passed full `ProofTTL Code Checks` run `33973853885`, including the new orchestration regression and the Wrangler Worker dry-run.
- Finalization found and closed an adjacent release-gate drift before stopping: the new regression initially ran only in Actions. Added first-class `test:evidence-orchestrator`, inserted it into `test:local`/`predeploy`, and changed Actions to invoke the same named target so direct deploy validation and CI enforce the same orchestration boundary.
- Rechecked `proofttl-web`: sprint remains at `ce43f9eb4000048547e3451aac836c68da0678dd`; no superficial web changes were justified by this core integration checkpoint.
- Remaining flagship boundary: real discovery/fetch/semantic/contradiction provider implementations and their pricing/configuration still need to be injected into this orchestrator from the public `/verify` path. This checkpoint creates and verifies the bounded execution path; it does not claim external providers are live yet.
