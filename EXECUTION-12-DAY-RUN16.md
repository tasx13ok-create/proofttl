# ProofTTL 12-Day Execution Log — Run 16

## 2026-09-05 EVIDENCE RECEIPT / PLAN BINDING CHECKPOINT

- Re-inspected the receipt-derived evidence execution boundary after the prior fail-closed coherence work and found an adjacent integrity gap: `summarizeEvidenceExecution` validated outer/inner receipt consistency and idempotency conflicts, but did not validate that a receipt kind was supported or that the set of settled/reserved receipts was possible under the Evidence Plan's per-kind execution ceilings.
- Hardened `src/evidence-execution-summary.js` so unsupported receipt kinds now fail closed and reservation-bearing receipts are counted by action kind against the plan's `max_candidate_queries`, `max_source_fetches`, `max_semantic_evaluations`, and `max_contradiction_queries` ceilings when an execution budget is present. Impossible receipt sets can no longer inflate `executed_action_count`, `completed_action_count`, or satisfy a contradiction pass that the plan could not have executed.
- Added `scripts/evidence-execution-plan-binding-test.js` covering unsupported kinds, a contradiction receipt under a zero-contradiction budget, source-fetch count overflow, and idempotent replay remaining count-stable.
- Found CI drift while finalizing: the sprint's `package.json` already had evidence-independence, evidence-execution-summary, and executed-verification-outcome tests, but the manually enumerated `ProofTTL Code Checks` core stage did not run all of them. Updated `.github/workflows/smoke.yml` so the branch gate now runs those evidence-runtime regressions plus the new plan-binding test before downstream commercial/platform, assistant/entitlement, readiness/auth/payment/research/release checks and Worker dry-run validation.
- Code commits in this checkpoint: `fac46ec83173c3c7b9b21fc5fc6f42646dfe4e82` (receipt/plan binding), `694ad76aa42ec55badb6a7b4c3414247be2f93ce` (regression), `0ebfcf82302df84681a898d247b82daf35b7c579` (CI gate coverage). ProofTTL Code Checks run `33959453650` was in progress at finalization, so this log does not claim green CI yet.
- Rechecked `proofttl-web/10xeffort-12-day-sprint`; it remains at `ce43f9eb4000048547e3451aac836c68da0678dd`, with no concrete web defect justifying churn in this pass.
- Remaining flagship runtime boundary: real provenance-preserving independent candidate discovery plus a genuinely separate adversarial contradiction retrieval pass still need to execute inside public `/verify` through the hardened evidence executor. This checkpoint strengthens the trust boundary around receipts; it does not claim those providers are live.
