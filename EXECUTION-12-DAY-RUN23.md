# ProofTTL 12-Day Execution Log — Run 23

## 2026-09-05 SOURCE-FETCH PROVENANCE / ZERO-BUDGET PRICING CHECKPOINT

- Re-inspected the evidence orchestrator after the prior provenance-hardening checkpoint and found a remaining fail-open source-binding path. `SOURCE_FETCH` accepted a provider result whose actual `source_url` differed from the discovered candidate if the provider merely copied the expected candidate URL into its own `requested_source_url` metadata. That allowed an unrelated fetched document to inherit discovery provenance.
- Tightened `SOURCE_FETCH` binding so the normalized provider-returned `source_url` itself must equal the normalized discovered candidate URL. Provider-declared `requested_source_url` no longer substitutes for actual source identity. Mismatches settle the action as failed with `SOURCE_FETCH_NOT_BOUND_TO_CANDIDATE`, and the verification outcome remains fail-closed rather than using the substituted source.
- Added a regression that deliberately returns an unrelated fetched URL while spoofing the expected URL in `requested_source_url`; the action must fail and the final verdict must remain `UNKNOWN`.
- Reassessed the adjacent runtime-budget materialization boundary and found an integration defect for plans that disable an action kind. Pricing was required for every action kind even when that action's plan limit was exactly zero, forcing callers to provide meaningless provider pricing for stages that cannot execute. Runtime materialization now assigns a zero reserve cost to zero-limit stages and does not require their pricing. Positive-limit stages still require finite non-negative pricing and continue to fail closed when pricing is missing or invalid.
- Added regression coverage for a plan with `max_contradiction_queries: 0` and no contradiction-provider price; the runtime budget materializes correctly with zero contradiction reserve cost while preserving the expected hard cost ceiling. Existing active-stage missing/invalid-pricing failures remain enforced.
- Full `ProofTTL Code Checks` are green at code checkpoint `b7742554d9492dd71da5f830648c0f9ddedbdfaa` (Actions run `33979724475`). Core security/limits/economics/lease/evidence checks, commercial/account/platform checks, assistant/entitlement checks, readiness/auth/routing/payment/research/regression checks, and Worker bundle validation all completed successfully.
- Final web/deployment recheck found no justified UI mutation: `proofttl-web/10xeffort-12-day-sprint` and web `main` remain identical at `ce43f9eb4000048547e3451aac836c68da0678dd`. The matching sprint preview `dpl_G9gffm5Dka7uMmLyxskp2PwYYGX9` and production deployment `dpl_6HELAY62mzgDuNRmQ8KNBCFrAXpK` are both `READY`.
- Remaining flagship boundary is unchanged in kind: public `/verify` still needs the real independent discovery, bounded source retrieval, exact-source semantic evaluation, and genuinely separate adversarial contradiction providers wired through the hardened executor. This checkpoint makes those future provider boundaries stricter and makes disabled stages easier to configure honestly; it does not claim the real providers are live.
