# ProofTTL 12-Day Execution Log — Run 24

## 2026-09-05 ADVERSARIAL RETRIEVAL PRESERVATION CHECKPOINT

- Re-inspected the receipt-backed evidence orchestration path after the source-binding hardening and found a correctness gap in bounded retrieval. Primary-discovery candidates are produced before adversarial contradiction candidates; the orchestrator then deduplicated that combined list and took the first `max_source_fetches` entries. A discovery provider returning more primary candidates than the fetch ceiling could therefore starve every adversarial candidate even though the required `CONTRADICTION_QUERY` itself completed.
- Tightened bounded source selection so a required contradiction pass with at least one returned adversarial candidate reserves retrieval capacity for an adversarial source instead of allowing primary candidates to consume every fetch slot. When capacity permits, the selector preserves both a primary source and an adversarial source first, then fills remaining slots from the original deduplicated ordering. Non-contradiction plans retain the original bounded-order behavior.
- This closes a concrete query-only contradiction path in the actual orchestrator: returned counterevidence can no longer disappear solely because primary discovery produced more URLs than the global fetch ceiling. Existing URL safety, exact fetch/source binding, provenance tagging, cost ceilings, idempotency, and semantic-evaluation limits remain unchanged.
- Added a regression where primary discovery returns twenty unique sources against the high-assurance sixteen-fetch ceiling while contradiction discovery returns a separate counterevidence URL. The test requires that counterevidence URL to survive bounded selection, be fetched, reach semantic evaluation, and appear in the evidence ledger with `ADVERSARIAL_CONTRADICTION` discovery provenance.
- Full `ProofTTL Code Checks` are green at code checkpoint `d7110c5f2d59d7cd86849c7b5f89b72fddae16ac` (Actions run `33982778550`). The checkpoint includes the new bounded-adversarial regression together with the existing evidence/security/economics/commercial/account/assistant/readiness/auth/payment/research/regression/release gates and Worker bundle validation.
- Final web/deployment recheck found no justified UI mutation: `proofttl-web/10xeffort-12-day-sprint` remains at `ce43f9eb4000048547e3451aac836c68da0678dd`, and the matching sprint preview `dpl_G9gffm5Dka7uMmLyxskp2PwYYGX9` plus production deployment `dpl_6HELAY62mzgDuNRmQ8KNBCFrAXpK` are both `READY`.
- Remaining flagship boundary is still the real provider integration: public `/verify` needs production independent discovery, bounded retrieval, exact-source semantic evaluation, and a genuinely separate adversarial contradiction provider wired through this hardened runtime. This checkpoint guarantees adversarial results are not silently starved once those providers are connected; it does not claim those live providers are already executing.