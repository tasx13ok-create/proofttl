# ProofTTL 12-Day Execution Log — Run 15

## 2026-09-05 FAIL-CLOSED EVIDENCE RECEIPT CHECKPOINT

- Re-inspected the new receipt-driven verification boundary before wiring real discovery providers. Found that the execution summarizer trusted the embedded reservation outcome without proving it agreed with the executor result status. A malformed `FAILED` receipt with a settled `COMPLETED` reservation could therefore be counted as a completed contradiction pass.
- Hardened `src/evidence-execution-summary.js` so receipt shape and state transitions fail closed. Result statuses are constrained to the executor contract; completed/failed/denied/replay receipts must agree with reservation state and outcome; budget denials must carry the expected denial version; mixed denial/reservation receipts are rejected; and an `ALREADY_RESERVED` replay is treated as unfinished execution rather than evidence that work completed.
- Found and closed an adjacent finalization gap: a planned verification with zero receipts was correctly summarized as `NOT_EXECUTED`, but `deriveExecutedVerificationOutcome(...)` previously discarded that status before final verdict construction. For plans without a mandatory contradiction pass, caller-supplied evidence could therefore still produce a reportable definitive verdict even though no planned action ran.
- Propagated receipt-derived `execution_status` into `finalizeVerificationOutcome(...)`. Any declared non-complete execution state now withholds the final verdict/confidence and reports `UNKNOWN`; the evidence-level verdict remains preserved for auditability. Invalid execution-status values are rejected rather than silently coerced.
- Added regressions for incoherent `FAILED`/`COMPLETED` receipts, invalid denial versions, mixed receipt outcomes, pending replay reservations, completed settled replays, and the zero-receipt planned-verification case. The zero-receipt test proves a strong `SUPPORTED` evidence ledger remains auditable but cannot become the final verdict when execution is `NOT_EXECUTED`.
- Final diff from the prior checkpoint is five coherent commits across only the receipt summary, final-outcome integration, and their two focused test files. No unrelated product or UI churn was introduced.
- Full `ProofTTL Code Checks` are green at code checkpoint `3f72e7d9c8b252cec2a25813187a00296163f550` (Actions run `33957406256`).
- Rechecked `proofttl-web`: sprint and `main` remain identical at `ce43f9eb4000048547e3451aac836c68da0678dd`. The matching sprint preview `dpl_G9gffm5Dka7uMmLyxskp2PwYYGX9` and production deployment `dpl_6HELAY62mzgDuNRmQ8KNBCFrAXpK` are both `READY`; no web change was justified by this core integrity pass.
- Remaining flagship boundary: public `/verify` still needs real provenance-preserving independent candidate discovery and a genuinely separate adversarial contradiction provider wired through the hardened executor/receipt path. Until those real provider actions settle successfully, ProofTTL must continue to report them as not executed rather than implying automated multi-source verification is live.
