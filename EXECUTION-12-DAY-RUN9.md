# ProofTTL 12-Day Execution Log — Run 9

## 2026-09-04 EVIDENCE INDEPENDENCE ORIGIN HARDENING CHECKPOINT

- Re-inspected the remaining `/verify` discovery boundary before adding external retrieval. Found a correctness defect in the evidence ledger: `independentGroupCount` preferred `underlying_source_id` over publisher origin, so two different pages or revisions from the same hostname could count as two independent corroborating sources whenever their content fingerprints differed.
- Hardened evidence independence semantics so accepted evidence is grouped by traceable source hostname first, publisher identity second, and only falls back to an underlying source identifier when neither origin is available. Distinct pages from one publisher can remain distinct ledger items and contribute to evidence strength, but they no longer manufacture independent-source corroboration.
- Preserved mirror protection: items sharing the same `underlying_source_id` are still deduplicated before independence counting, so copies of one report across different hosts cannot manufacture additional origins.
- Added `scripts/evidence-independence-test.js` with regressions proving (1) same-host pages with different fingerprints count as one independent group, (2) separate publisher hosts count separately, and (3) mirrored copies sharing one underlying source remain deduplicated. Added the regression to `test:local`, so the direct predeploy gate and GitHub Code Checks exercise the same boundary.
- Full `ProofTTL Code Checks` are green at code checkpoint `5790832f18ed9398f1db33fbb3affaabdf10bfab` (Actions run `33928288171`).
- Rechecked the web launch surface without introducing cosmetic churn: `proofttl-web/10xeffort-12-day-sprint` and `main` remain identical at `ce43f9eb4000048547e3451aac836c68da0678dd`; the current sprint preview and production deployments for that commit are both READY.
- Remaining truth boundary: public `/verify` still does not execute real provenance-preserving independent-source discovery plus an adversarial contradiction retrieval pass. This checkpoint prevents the future discovery layer from overstating independence once multiple same-publisher pages enter the ledger; it does not claim multi-source retrieval is live.
- Residual identity limitation: hostname grouping is stricter than fingerprint grouping but does not yet collapse sibling subdomains that may share one controlling publisher. A future discovery/origin-normalization pass should carry explicit publisher organization identity rather than inferring control solely from hostnames.
