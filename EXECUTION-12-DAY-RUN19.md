# ProofTTL 12-Day Execution — Run 19

## 2026-09-05 RUNTIME EVIDENCE BUDGET MATERIALIZATION CHECKPOINT

- Re-inspected the Evidence Plan -> executor boundary and found a real orchestration blocker: every executable plan intentionally carries `hard_cost_ceiling_usd: null` with `hard_cost_ceiling_state: REQUIRES_RUNTIME_PRICING_ENFORCEMENT`, while `createEvidenceExecutor` correctly rejects any non-finite hard cost ceiling. Passing a signed/declarative Evidence Plan directly into the executor therefore cannot run and would throw before candidate discovery.
- Added `materializeEvidenceExecutionBudget(...)` as the explicit bridge between declarative planning and executable provider work. It requires finite non-negative reserve pricing for each action class, validates all plan action ceilings and latency budget, computes the plan's worst-case reserved spend, and emits an executor-compatible finite `hard_cost_ceiling_usd` plus the exact per-action reserve costs that orchestration must use.
- The materializer never silently invents provider prices and refuses `NOT_SCHEDULED` plans. A configured runtime cost cap may only tighten the computed worst-case ceiling; it cannot expand the plan beyond its priced worst case. This preserves fail-closed accounting and keeps runtime economics separate from the signed evidence-planning contract.
- Added regressions for deterministic worst-case cost calculation, explicit per-action reserve prices, configured hard-cap tightening, rejection of non-executable plans, missing pricing, negative pricing, and invalid configured ceilings. Added that regression directly to the staged `ProofTTL Code Checks` core evidence gate so provider orchestration cannot land without validating this boundary.
- No provider/discovery capability is claimed live by this checkpoint. Public `/verify` remains caller-source-only and final outcomes continue to fail closed until real discovery/fetch/semantic/contradiction receipts exist. This change removes the runtime-pricing incompatibility that would otherwise prevent the hardened executor from being wired safely.
- `proofttl-web/10xeffort-12-day-sprint` remains unchanged at `ce43f9eb4000048547e3451aac836c68da0678dd`; no unrelated UI churn was introduced.
- Final verification target for this checkpoint is the full `ProofTTL Code Checks` run on branch head after the CI-gate update. If green, the next highest-value integration step is to supply real provider pricing/configuration and orchestrate `CANDIDATE_QUERY -> SOURCE_FETCH -> SEMANTIC_EVALUATION`, plus a genuinely separate `CONTRADICTION_QUERY` when required, through this finite runtime budget.
