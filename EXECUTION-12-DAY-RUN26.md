# ProofTTL 12-Day Execution Log — Run 26

## 2026-09-05 EVIDENCE TIMESTAMP INTEGRITY CHECKPOINT

- Re-inspected the evidence scoring boundary after the cost-capped fetch -> semantic pipeline work and found a temporal-integrity correctness gap. `assessEvidence()` previously used `safeDate(input.observed_at) || new Date()`, so a malformed provider-supplied observation timestamp could silently become the current time and receive fresh-evidence treatment. A malformed publication/update timestamp likewise degraded to an omitted date and inherited unknown-date freshness rather than being identified as invalid metadata.
- Hardened `src/evidence-quality.js` so explicitly supplied but unparsable observation and publication/update timestamps make the evidence item ineligible for the accepted ledger. Missing timestamps retain the existing compatibility behavior; malformed timestamps now fail closed with explicit `REJECTED_INVALID_OBSERVATION_TIMESTAMP` or `REJECTED_INVALID_PUBLICATION_TIMESTAMP` reasons instead of silently acquiring trustworthy temporal semantics.
- Added verification-primitive regressions using otherwise maximum-quality, traceable, verbatim evidence to prove that malformed observation and publication timestamps are still rejected. This gate is already part of `npm run test:local`, and therefore also the direct `predeploy` validation path and GitHub Code Checks.
- Full `ProofTTL Code Checks` are green at substantive checkpoint `24dd97feb403413fe14419cb8733e0c65b6fbcb0` (Actions run `33988859023`). The checkpoint includes both the production hardening commit and its regression coverage.
- Re-inspected `proofttl-web`: `10xeffort-12-day-sprint` and `main` remain identical at `ce43f9eb4000048547e3451aac836c68da0678dd`. Fresh Vercel inspection confirms the matching sprint preview `dpl_G9gffm5Dka7uMmLyxskp2PwYYGX9` and production deployment `dpl_6HELAY62mzgDuNRmQ8KNBCFrAXpK` are both `READY`.
- Production runtime error aggregation for `proofttl-web` over the last 24 hours shows one recurring Node `DEP0169` `url.parse()` deprecation-warning group on `/api/auth-proxy` and `/api/runtime-proxy`, not an application request-failure group. It remains worth removing when the owning dependency/path is isolated, but it did not justify unrelated proxy churn during this evidence-integrity pass.
- Remaining flagship boundary is intentionally unchanged: public `/verify` still needs production independent discovery, bounded retrieval, exact-source semantic evaluation, real runtime provider pricing/configuration, and a genuinely separate adversarial contradiction provider wired through the hardened executor before automated receipt-backed evidence execution can be claimed there.
