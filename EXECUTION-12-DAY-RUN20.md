# ProofTTL 12-Day Execution — Run 20

## 2026-09-05 EVIDENCE DEPLOY-GATE COHERENCE CHECKPOINT

- Re-inspected the runtime-pricing checkpoint after its branch CI completed successfully and found a release-gate split: `scripts/evidence-runtime-budget-test.js` had been added to the staged GitHub Actions command, but `npm run test:local` remained unaware of it. Because `predeploy` delegates to `test:local`, a direct `npm run deploy` could bypass the exact regression that validates the Evidence Plan -> finite executor-budget bridge.
- The adjacent evidence execution plan-binding regression had the same defect: Actions invoked `node scripts/evidence-execution-plan-binding-test.js` directly, but the deploy/predeploy path did not run it at all. This is a correctness/reliability issue, not a cosmetic one: plan-binding prevents impossible or over-budget receipt sets from being treated as valid executed verification.
- Added first-class `test:evidence-runtime-budget` and `test:evidence-execution-plan-binding` package scripts and inserted both into `test:local`. `predeploy`, local release validation, and CI now enforce the same two load-bearing evidence-runtime boundaries.
- Updated the staged `ProofTTL Code Checks` workflow to invoke the same named package targets rather than bespoke raw `node` commands, reducing future drift between CI and direct deploy validation.
- Full `ProofTTL Code Checks` passed at code checkpoint `c5f45f3dbfc4946969edc0f2420bded8279ff677` (Actions run `33970559608`), including the newly unified evidence runtime-budget and plan-binding gates plus the existing Worker dry-run.
- Final diff review from prior sprint head `9ca02774c9292e12699051ec0610415f3ffdb10d` to the green code checkpoint is limited to `.github/workflows/smoke.yml` and `package.json`: two commits, three package-script additions, one `test:local` expansion, and replacement of the two raw workflow invocations with their named equivalents. No production runtime or unrelated product/UI code changed.
- Rechecked `proofttl-web`: sprint preview and production still point at identical commit `ce43f9eb4000048547e3451aac836c68da0678dd`; Vercel reports preview deployment `dpl_G9gffm5Dka7uMmLyxskp2PwYYGX9` READY and production deployment `dpl_6HELAY62mzgDuNRmQ8KNBCFrAXpK` READY. No superficial web changes were introduced.
- Flagship boundary remains explicit: public `/verify` still does not execute real independent candidate discovery, bounded source fetching, semantic evaluation, or a genuinely separate adversarial contradiction provider. The next integration step should wire real provider configuration/pricing and orchestration through the now-consistent predeploy/CI evidence gates without weakening receipt-derived fail-closed verdict semantics.
