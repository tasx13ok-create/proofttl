# ProofTTL 12-Day Execution Log — Run 28

## 2026-09-05 DISCOVERY INTENT LINEAGE CHECKPOINT

- Re-inspected the hardened evidence orchestrator and found an auditability gap in the flagship plan → execution → evidence chain: accepted evidence recorded only a broad `PRIMARY_DISCOVERY` or `ADVERSARIAL_CONTRADICTION` label, so a later reviewer could not determine which concrete planned query intent actually discovered a source when multiple primary intents were executed.
- Hardened `src/evidence-orchestrator.js` so every discovered candidate carries the originating query intent purpose and bounded intent index. The lineage is preserved through candidate dedupe/selection, source fetch, semantic evaluation, and into the final evidence item's provenance as `discovery_intent_purpose` and `discovery_intent_index`.
- The existing adversarial-dedupe behavior remains intact: if the same URL is discovered by primary and contradiction searches, it remains one source origin and is upgraded to the adversarial discovery path together with the adversarial intent lineage rather than retaining stale primary lineage.
- Added `scripts/evidence-intent-lineage-test.js`, which executes the generated high-assurance plan through primary discovery, contradiction discovery, fetch, and semantic evaluation and proves both primary and adversarial evidence retain their exact discovery intent metadata. Added the regression to `npm run test:local`, so it is enforced by direct `predeploy` validation as well as CI.
- Full `ProofTTL Code Checks` are green at substantive checkpoint `981d392d7f1326b97eefc0b024212337e3d6bbd1` (Actions run `33994826372`). All grouped test stages completed successfully, including core security/economics/lease primitives, commercial/account/platform primitives, assistant/entitlement primitives, readiness/auth/routing/payment/research/regression checks, and Worker bundle validation.
- Final diff review from prior sprint head `0e66830276fc0a044d98906098fbfcc7c26aa5cb` to the green checkpoint is focused and reversible: 3 commits touching only `src/evidence-orchestrator.js`, the new targeted regression, and `package.json` (97 changed lines total: 89 additions, 8 deletions).
- Re-inspected `proofttl-web`: the latest sprint preview remains `dpl_G9gffm5Dka7uMmLyxskp2PwYYGX9` and is `READY`; production remains `dpl_6HELAY62mzgDuNRmQ8KNBCFrAXpK` and is `READY`. Both correspond to `ce43f9eb4000048547e3451aac836c68da0678dd`, so no unrelated web/deployment drift was introduced during this backend pass.
- Remaining flagship boundary is unchanged: public `/verify` still needs real independent discovery, bounded retrieval, exact-source semantic evaluation, real provider pricing/configuration, and a genuinely separate adversarial contradiction provider wired through the hardened executor. The runtime now has stronger source-to-plan lineage ready for that integration and still should not advertise receipt-backed automated evidence execution before the providers are actually connected.
