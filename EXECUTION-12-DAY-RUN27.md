# ProofTTL 12-Day Execution Log — Run 27

## 2026-09-05 ADVERSARIAL DISCOVERY PROVENANCE DEDUPE CHECKPOINT

- Re-inspected the evidence orchestration path after the timestamp-integrity work and found a cross-discovery provenance bug: primary candidate discovery is appended before the independent contradiction search, while URL deduplication previously kept the first normalized URL only. If both providers returned the same source URL, the source remained one origin as intended but silently lost its `ADVERSARIAL_CONTRADICTION` discovery provenance.
- Hardened `src/evidence-orchestrator.js` so duplicate normalized URLs are still represented as one source origin, but a later adversarial discovery upgrades the retained candidate's discovery provenance instead of being discarded. This preserves the independent contradiction-search path through bounded candidate selection, source fetch, semantic evaluation, and the evidence ledger without double-counting the source.
- Added an orchestrator regression where normal discovery and contradiction discovery deliberately return the same URL. The test proves the source is fetched/evaluated only once, remains one evidence origin, and carries `ADVERSARIAL_CONTRADICTION` provenance through fetch and semantic evaluation.
- Full `ProofTTL Code Checks` are green at substantive checkpoint `b0a3451e2590d45fb97b1aa3e5d7357629a81228` (Actions run `33991921024`). Core security/economics/lease gates, commercial/account/platform gates, assistant/entitlement gates, readiness/auth/routing/payment/research/regression gates, and Worker bundle validation all completed successfully.
- Final diff review from prior sprint head `03f08366b21bed3ae60240f8029b6c1b6696a863` to the green checkpoint contains only `src/evidence-orchestrator.js` and `scripts/evidence-orchestrator-test.js` (2 commits; 57 changed lines total), keeping the production change focused and reversible.
- Re-inspected `proofttl-web`: `10xeffort-12-day-sprint` remains at `ce43f9eb4000048547e3451aac836c68da0678dd`. Fresh Vercel inspection confirms the matching sprint preview `dpl_G9gffm5Dka7uMmLyxskp2PwYYGX9` and production deployment `dpl_6HELAY62mzgDuNRmQ8KNBCFrAXpK` are both `READY` and both correspond to `ce43f9eb4000048547e3451aac836c68da0678dd`; no unrelated web churn was introduced.
- Remaining flagship boundary is intentionally unchanged: public `/verify` still needs real independent discovery, bounded retrieval, exact-source semantic evaluation, real runtime provider pricing/configuration, and a genuinely separate adversarial contradiction provider wired through the hardened executor before automated receipt-backed evidence execution can be claimed there.
