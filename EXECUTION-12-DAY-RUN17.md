# ProofTTL 12-Day Execution — Run 17

## 2026-09-05 CALLER-SOURCE EXECUTION TRUTH CHECKPOINT

- Re-inspected the receipt-derived Evidence Plan boundary and found a remaining public-path split-brain. `attachImmutableVerificationContext` correctly records the public `/verify` Evidence Plan as `NOT_EXECUTED_BY_PUBLIC_VERIFY` with zero executed actions, but the legacy caller-provided-source outcome path did not pass an execution status into `finalizeVerificationOutcome`. For ordinary claims where no contradiction pass was required, the finalizer could therefore default the verification execution to `COMPLETE` and publish a definitive `SUPPORTED`/`CONTRADICTED` final result even while the adjacent signed plan truthfully said planned independent discovery had not executed.
- Closed that gap in `src/verification-context.js`. The caller-source issuance and changed-source reverification path now declares `execution_status: NOT_EXECUTED` and never treats the caller-provided source as a completed contradiction pass. Its source-level semantic result remains preserved in `source_verdict` and the ledger's `evidence_verdict`, but the final published verdict is `UNKNOWN` and overall confidence is withheld until the planned evidence runtime executes through receipt-backed orchestration.
- This applies to ordinary as well as high-assurance claims. High-assurance claims retain the more specific `CONTRADICTION_PASS_INCOMPLETE` status through finalizer precedence; ordinary planned claims now correctly report `NOT_EXECUTED` instead of silently presenting a complete verification.
- Expanded `scripts/verification-context-test.js` to assert ordinary caller-source issuance is final `UNKNOWN`/`NOT_EXECUTED`, status aliases stay aligned, confidence is withheld, and an ordinary changed-source scheduled reverification obeys the same fail-closed standard while preserving its provisional source/evidence verdict.
- Final code checkpoint for this slice is `8c7877d97ff07cad181ef1b0fd2b9821e3666c65`. Full `ProofTTL Code Checks` run `33964971592` passed: core security/limits/economics/lease primitives, commercial/account/platform primitives, assistant/entitlements, readiness/auth/routing/payment/research/regression checks, and Worker bundle validation all completed successfully.
- Rechecked `proofttl-web`: sprint and `main` remain identical at `ce43f9eb4000048547e3451aac836c68da0678dd`. Live Vercel inspection confirms the matching sprint preview `dpl_G9gffm5Dka7uMmLyxskp2PwYYGX9` and production deployment `dpl_6HELAY62mzgDuNRmQ8KNBCFrAXpK` are both `READY`, so no cosmetic or unrelated web churn was introduced.
- Diff coherence/reversibility: this run changes only the caller-source final-outcome execution declaration, the directly corresponding verification-context regressions, and this execution log. It does not add a provider, broaden claims, alter evidence acceptance thresholds, or pretend discovery has shipped.
- Remaining flagship truth boundary: public `/verify` must actually execute provenance-preserving independent candidate discovery, bounded source fetch/semantic evaluation, and (when required) a genuinely separate adversarial contradiction retrieval pass through the budgeted executor. Definitive automated final verdicts must only return once those planned stages have valid receipts.
